
  # Prototipo evento

  This is a code bundle for Prototipo evento. The original project is available at https://www.figma.com/design/e1CCS504xwjaNCYSl9761H/Prototipo-evento.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Prototipe0.1
